---
interface Props {
	title: string;
	body: string;
	image?: string
}

const { title, body, image } = Astro.props;
---

<div class="bg-gray-700 my-2 p-4 rounded-xl border-gray-900 border-1">
		<h2 class="font-semibold text-gray-300">
			{title}
		</h2>
		<p class="text-gray-500">
			{body}
		</p>
		{image && <img src={image} /> }
		
</div>

